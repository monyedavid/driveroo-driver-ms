{
    "private": true,
    "scripts": {
        "start": "lerna run start",
        "start:dev": "lerna run --stream start:dev",
        "build:server": "lerna run build:server",
        "gen-schema-types": "lerna run gen-schema-types"
    },
    "workspaces": [
        "packages/*"
    ],
    "name": "driveroo",
    "devDependencies": {
        "lerna": "^3.16.4"
    }
}
